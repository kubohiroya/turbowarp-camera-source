{
  "name": "@kubohiroya/turbowarp-camera-source",
  "version": "0.2.0",
  "author": "Hiroya Kubo <hiroya@cuc.ac.jp>",
  "description": "A TurboWarp capability extension for sharing one MediaDevices camera stream across extensions.",
  "type": "module",
  "license": "MPL-2.0",
  "homepage": "https://kubohiroya.github.io/turbowarp-camera-source/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kubohiroya/turbowarp-camera-source.git"
  },
  "bugs": {
    "url": "https://github.com/kubohiroya/turbowarp-camera-source/issues"
  },
  "exports": {
    "./camera-source.js": "./dist/camera-source.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist/",
    "README.md",
    "README.ja.md",
    "LICENSE"
  ],
  "unpkg": "./dist/camera-source.js",
  "jsdelivr": "./dist/camera-source.js",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "lint": "eslint src tests vite.config.ts",
    "test": "vitest run",
    "docs": "node scripts/generate-readme.ts",
    "docs:check": "pnpm run docs && git diff --exit-code -- README.md",
    "docs:pages:check": "node scripts/check-pages.ts",
    "repo:check": "node scripts/check-repo.ts",
    "pack:check": "npm pack --dry-run --ignore-scripts",
    "release:check": "pnpm publish --dry-run --access public --no-git-checks",
    "check": "pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run docs:check && pnpm run docs:pages:check && pnpm run check:dist && pnpm run repo:check && pnpm run pack:check",
    "check:dist": "pnpm run build && node scripts/check-dist.ts",
    "prepack": "pnpm run check"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@kubohiroya/vite-plugin-turbowarp-extension": "0.1.1",
    "@types/node": "^24.13.3",
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.66.0",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  },
  "keywords": [
    "turbowarp",
    "scratch",
    "extension",
    "camera",
    "media-devices",
    "getusermedia"
  ]
}
